[project]
name = "la-crater-analyzer"
version = "0.1.0"
description = "GUI tool for annotating laser-ablation crater videos."
requires-python = ">=3.11"
dependencies = [
    "PySide6>=6.7,<6.8",
    "opencv-python>=4.9",
    "numpy>=1.26",
    "matplotlib>=3.8",
]

[tool.ruff]
line-length = 100
